devtools::load_all(".")
devtools::test()
devtools::load_all(".")
devtools::test()
install.packages('gganimate')
install.packages('gganimate')
install.packages("sf")
g <- graph_from_literal(1--2, 1--3, 1--4)
summary(g)
E(g)$weight <- c(-1, -2, -1) # negative weights
E(g)$weight
spreadr(g, data.frame(node = 1, activation = 100), time = 1) # spreadr behaves as if edge weights are positive
E(g)$weight > 0
?assert_that
assert_that(E(g)$weight > 0)
E(g)$weight > 0 |> all()
E(g)$weight > 0 == TRUE
is.true(E(g)$weight > 0)
E(g)$weight > 0 |> all()
all(E(g)$weight > 0)
devtools::load_all(".")
E(g)$weight
spreadr(g, data.frame(node = 1, activation = 100), time = 1) # spreadr behaves as if edge weights are positive
devtools::document()
devtools::load_all(".")
use_test('test-negative-edges')
testthat::use_test('test-negative-edges')
usethis::use_test('test-negative-edges')
devtools::load_all(".")
usethis::use_logo('spreadr-out2.png')
use_readme_rmd()
usethis::use_readme_rmd()
devtools::build_readme()
usethis::use_pkgdown()
pkgdown::build_site()
devtools::install()
pkgdown::build_site()
g <- graph_from_literal(1--2, 1--3, 1--4)
E(g)$weight <- c(-1, -2, -1)
E(network)$weight >= 0
E(g)$weight >= 0
g <- graph_from_literal(1--2, 1--3, 1--4)
E(g)$weight <- c(-1, -2, 0)
E(g)$weight >= 0
all(E(g)$weight >= 0)
devtools::load_all(".")
devtools::install()
pkgdown::build_site()
weighted_network <- matrix(
c(0, 1, 9,
1, 0, 0,
9, 0, 0), nrow=3, byrow=TRUE)
colnames(weighted_network) <- c("a", "b", "c")
rownames(weighted_network) <- c("a", "b", "c")
# To visualise the network only --- this is not necessary for spreadr
weighted_igraph <- graph_from_adjacency_matrix(
weighted_network, mode="undirected", weighted=TRUE)
plot(weighted_igraph, edge.width=E(weighted_igraph)$weight)
spreadr(
weighted_network, data.frame(node="a", activation=10),
time=1, retention=0, include_t0=TRUE)
spreadr(
weighted_igraph, data.frame(node="a", activation=10),
time=1, retention=0, include_t0=TRUE)
pkgdown::build_site()
spreadr(weighted_network, data.frame(node = 1, activation = 100), time = 1)
assert_that(is_igraph(weighted_network) ||
length(dim(network)) == 2 && dim(network)[1] == dim(network)[2])
assertthat::assert_that(is_igraph(weighted_network) ||
length(dim(network)) == 2 && dim(network)[1] == dim(network)[2])
assertthat::assert_that(is_igraph(weighted_network) ||
length(dim(weighted_network)) == 2 && dim(weighted_network)[1] == dim(weighted_network)[2])
rlang::last_trace()
network <- weighted_igraph # when network is igraph
assertthat::assert_that(is_igraph(network) && all(E(network)$weight >= 0))
network <- weighted_network # when network is matrix
network >= 0
all(network >= 0)
network <- weighted_igraph # when network is igraph
is_weighted(network)
g <- graph_from_literal(1-2)
is_weighted(g)
?assert_that
E(g)$weight
E(g)$weight >= 0
all(E(g)$weight >= 0)
all(E(network)$weight >= 0)
g
network
devtools::load_all(".")
g <- graph_from_literal(1--2, 1--3, 1--4)
E(g)$weight <- c(-1, -2, -1) # negative weights
expect_snapshot_error(
spreadr(g, data.frame(node = 1, activation = 100), time = 1)
)
spreadr(g, data.frame(node = 1, activation = 100), time = 1)
g
E(g)$weight
rlang::last_trace()
devtools::load_all(".")
spreadr(g, data.frame(node = 1, activation = 100), time = 1)
g
network <- matrix(
c(0, 1,
0, 0), nrow=2, byrow=TRUE)
network
all(network >= 0)
g <- graph_from_literal(1--2, 1--3, 1--4)
E(g)$weight <- c(-1, -2, -1) # negative weights
network <- g
if (is_igraph(network) && is.null(E(network)$weight))
network <- as_adjacency_matrix(network)
else if (is_igraph(network) && !is.null(E(network)$weight))
if (is_igraph(network) && is.null(E(network)$weight))
network <- as_adjacency_matrix(network)
else if (is_igraph(network) && !is.null(E(network)$weight))
g <- graph_from_literal(1--2, 1--3, 1--4)
E(g)$weight <- c(-1, -2, -1) # negative weights
network <- g
if (is_igraph(network) && !is.null(E(network)$weight))
network <- as_adjacency_matrix(network, attr="weight")
network
all(network >= 0)
g <- graph_from_literal(1--2, 1--3, 1--4)
E(g)$weight <- c(-1, -2, -1) # negative weights
network <- g
if (is_igraph(network) && is.null(E(network)$weight))
network <- as_adjacency_matrix(network)
network
g <- graph_from_literal(1--2, 1--3, 1--4)
network <- g
if (is_igraph(network) && is.null(E(network)$weight))
network <- as_adjacency_matrix(network)
network
all(network >= 0)
devtools::load_all(".")
network
spreadr(network, data.frame(node = 1, activation = 100), time = 1)
g <- graph_from_literal(1--2, 1--3, 1--4)
E(g)$weight <- c(-1, -2, -1) # negative weights
if (is_igraph(network) && !is.null(E(network)$weight))
network <- as_adjacency_matrix(network, attr="weight")
network
g <- graph_from_literal(1--2, 1--3, 1--4)
E(g)$weight <- c(-1, -2, -1) # negative weights
network <- g
if (is_igraph(network) && !is.null(E(network)$weight))
network
g <- graph_from_literal(1--2, 1--3, 1--4)
E(g)$weight <- c(-1, -2, -1) # negative weights
network <- g
g <- graph_from_literal(1--2, 1--3, 1--4)
E(g)$weight <- c(-1, -2, -1) # negative weights
network <- g
network
E(network)$weight
if (is_igraph(network) && !is.null(E(network)$weight))
network <- as_adjacency_matrix(network, attr="weight")
network
spreadr(network, data.frame(node = 1, activation = 100), time = 1)
assert_that(all(network >= 0))
assert_that(all(as.matrix(network) >= 0))
devtools::load_all(".")
testthat::snapshot_accept("test-negative-edges")
devtools::install()
pkgdown::build_site()
usethis::use_pkgdown_github_pages()
usethis::use_pkgdown_github_pages()
